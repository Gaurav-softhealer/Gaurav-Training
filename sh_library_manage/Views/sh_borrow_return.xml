<?xml version="1.0" encoding="utf-8"?>

<odoo>

<!-- list view -->
<!-- 
    <record id="rejection_reason_list" model="ir.ui.view">
        <field name="name">rejection_reason_list</field>
        <field name="model">sh.rejection.reason</field>
        <field name="arch" type="xml">
            <list>

                <field name="name"/>

            </list>
        </field>
    </record> -->

<!-- form view -->

    <record id="borrow_return_form" model="ir.ui.view">
        <field name="name">borrow_return_form</field>
        <field name="model">sh.borrow.return</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                       <group>             
                          
                            <!-- <field name="book_ref_ids" /> -->
                            <field name="name" widget="many2many_tags"/>

                        </group>
                    </group>
                </sheet>
                        <footer>
                            <button name="return_book" string="Return" type="object" class="btn-primary" data-hotkey="q"/>
                            <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x" />
                        </footer>
            </form>    
        </field>
    </record>

<!-- action -->
    <record id="borrow_return" model="ir.actions.act_window">
        <field name="name">borrow_return</field>
        <field name="res_model">sh.borrow.return</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            No product found. Let's create one!
          </p><p>
            You must define a product for everything you sell or purchase,
            whether it's a storable product, a consumable or a service.
          </p>
        </field>
    </record>
  
</odoo>