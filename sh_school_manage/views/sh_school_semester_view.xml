<?xml version="1.0" encoding="utf-8"?>

<odoo>

<!-- list view -->

    <record id="semester_list" model="ir.ui.view">
        <field name="name">semester.list</field>
        <field name="model">sh.school.semester</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

<!-- form view -->

    <record id="semester_form" model="ir.ui.view">
        <field name="name">semester.form</field>
        <field name="model">sh.school.semester</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name" required="1"/>
                        <field name="sub_ids"/>
                    </group>
                </sheet>
            </form>    
        </field>
    </record>

<!-- action -->
    <record id="semester_action" model="ir.actions.act_window">
        <field name="name">school semester</field>
        <field name="res_model">sh.school.semester</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            No product found. Let's create one!
          </p><p>
            You must define a product for everything you sell or purchase,
            whether it's a storable product, a consumable or a service.
          </p>
        </field>
    </record>
  
</odoo>